language: node_js
sudo: required
services:
  - dockerfile

before_install:
  - docker build -t vermishka/docker-react -f Dockerfile.dev .

script:
  - docker run vermishka/docker-react npm run test -- --coverage